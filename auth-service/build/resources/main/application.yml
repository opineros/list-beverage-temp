server:
  port: 8081

app:
  jwtSecret: ${APP_JWTSECRET:nruA/RNh4Xgw7ZiFoGC0Wmd4glv2lK0E0LIWU8EMnMZe5Yt/NV7YrCYmmmpZHWn+x7qztGxbCy/eeVziqwZifA==}
  jwtExpirationMs: 3600000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: auth-service   # nombre único en el registro
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/authdb}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:p4Y2!qmvDTJd3JzZbjz9}
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
      scripts:
        - classpath:init.sql
  cloud:
    stream:
      bindings:
        # binding de salida: user-created-out-0
        user-created-out-0:
          destination: user.created
          content-type: application/json
      rabbit:
        bindings:
          user-created-out-0:
            producer:
              exchangeType: topic

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # URL de tu Eureka Server
    register-with-eureka: true    # permite que este servicio se registre
    fetch-registry: true          # permite que vea otros servicios